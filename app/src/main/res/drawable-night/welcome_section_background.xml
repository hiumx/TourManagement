<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="@color/primary_color_dark"
        android:centerColor="@color/gradient_start_dark"
        android:endColor="@color/gradient_end_dark"
        android:angle="45" />

    <!-- Add subtle overlay for depth -->
    <stroke
        android:width="1dp"
        android:color="@color/primary_light_dark" />
</shape>

